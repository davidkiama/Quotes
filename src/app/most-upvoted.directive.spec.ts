import { MostUpvotedDirective } from './most-upvoted.directive';

describe('MostUpvotedDirective', () => {
  it('should create an instance', () => {
    const directive = new MostUpvotedDirective();
    expect(directive).toBeTruthy();
  });
});
